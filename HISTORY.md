# Site — журнал правок

> Исторические записи о правках сайта. Инструкции — в README.md.

## Журнал чистки `assets/style.css`

Файл собирался в разное время, поэтому в нём накопился мусор. Чистка
ведётся аккуратно, по одной категории за раз, с проверкой баланса скобок
после каждого шага — не одним махом.

### Категория 1 — пустые комментарии-заголовки (сделано)

В файле было несколько мест вида:
```css
/* ── Cookie banner ── */



```
— заголовок-комментарий, под которым когда-то было CSS-правило, но правило
удалили, а сам заголовок и пустые строки после него — забыли. Визуально на
сайте это ни на что не влияло (браузер их просто не видит), но засоряло
файл и сбивало с толку при чтении: заголовок обещает код, а под ним пусто.

Удалено 5 таких блоков (заголовки: «Cookie banner» ×2, «Кнопка наверх»,
«МОБИЛЬНОЕ МЕНЮ» + «Оверлей за меню», «Telegram виджет») — итого 97 пустых
строк. **Ни одна строка с реальным CSS-правилом не тронута** — проверено
построчным сравнением с версией до правки: единственное отличие —
отсутствующие заголовки и пустые строки, весь код идентичен.
`693 → 596` строк.

### Категория 2 — дублирующиеся объявления (сделано)

Разбиралось по одному селектору за раз: правка → проверка баланса скобок →
проверка вычисленного эффективного стиля (до/после, программно) → следующий.
Ничего не удалялось массово без проверки.

**Итог — почищено:**

- `:root{--ink:...}` — дубль с идентичными значениями. Убран.
- `.logo{...}` — второе объявление было подмножеством первого (не хватало
  `color`/`text-decoration`, остальное совпадало). Убрано — эффект не изменился,
  недостающие свойства и так наследовались от первого правила.
- `@media(max-width:760px){.foot-grid{...}}` — сжатая копия существующего
  развёрнутого блока. Убрана.
- `@keyframes rise` — было два объявления с **разными** значениями (18px и
  16px). У `@keyframes` (в отличие от обычных правил) при совпадении имени
  побеждает целиком последнее по порядку объявление — так что 18px-версия
  была мёртвым кодом, реально анимация всегда шла с 16px. Убрано недостижимое.
- `a{color:inherit}`, `.logo small`, `.sec-head h2` — точные дубли или строгие
  подмножества. Убраны.
- `html{...}`, `body{max-width:100vw}` — по два-три объявления, часть друг
  друга полностью перекрывала (те же свойства, те же значения). Убраны лишние.
- `.sec-head::after` — было три объявления: одно без `content` (само по себе
  ничего не рисует), одно полное, одно — часть полного с теми же значениями.
  Проверено вручную, что итоговый вычисленный стиль одинаков что с одним
  полным правилом, что со всеми тремя. Оставлено одно полное.

**Сознательно не тронуто** (не дубли, а разные свойства одного селектора,
специально разбито на несколько правил — трогать не нужно):

- `footer a{color:var(--sand)}` — отдельной строкой под комментарием
  «Footer и nav — оставляем их собственные цвета», это фиксация решения.
- `nav` (position/z-index/background в одном правиле, padding — в другом)
- `.ph` (overflow/background в одном правиле, cursor — в другом)

`592 → 580` строк после этой категории (693 → 580 суммарно с категорией 1).


## Правило кэш-бастинга статики (`aelita-v13`)

`assets/style.css` и `assets/main.js` теперь подключаются с `?v=N`
(`href="/assets/style.css?v=13"`), как на сайте «Короче» — раньше на
Аэлите этого не было: браузер кэшировал CSS/JS без какого-либо
механизма инвалидации, и вернувшийся пользователь мог сколько угодно
долго видеть старые стили после правки. При каждой новой версии сайта:

1. Увеличить `?v=N` во всех 38 страницах (обе ссылки — на `style.css`
   и на `main.js`).
2. Синхронно поднять `SITE_VERSION` в `sw.js` (см. `## Service Worker`
   ниже) — так `CACHE_NAME`/`STATIC_CACHE` растут вместе с версией
   сайта, и `activate` в service worker'е реально вычищает старые
   кэши, а не просто существует формально.

## Service Worker (`aelita-v13`)

`CACHE_NAME`/`STATIC_CACHE` в `sw.js` не менялись с самого первого
релиза (`aelita-v1`) — из-за этого чистка старых кэшей в `activate`
не имела эффекта: имя кэша не менялось, значит и «старых» кэшей для
удаления не находилось. Заведена переменная `SITE_VERSION` в начале
`sw.js`, от которой считаются оба имени кэша и версия prefetch-ссылок
на `style.css`/`main.js` — теперь достаточно поднять одно число при
каждом релизе (см. правило кэш-бастинга выше).

## Яндекс.Метрика на `/app` (`aelita-v13`)

Счётчик стоял на всех 38 страницах, кроме `/app` (сам `/app` есть в
`sitemap.xml`, то есть индексируется и должен отслеживаться). Добавлен
тот же сниппет, что и на остальных страницах.

## Правки по итогам сверки с паком «Короче» (`aelita-v13`)

Прислан независимый пак материалов «Короче» — сверил факты о вечере
«Археология человеческого» построчно с их сайтом (страницы `/shows`,
`/gender`, `/ierarhiya`, `/ritual`, `/sachkov`) и поправил везде, где
Аэлита расходилась:

- Возраст 18+ → **12+** (везде: `/human`, `Human.pptx`, `AELITA_final.pptx`).
- «Вечер современного балета» / «Балет» → **«Вечер малых форм
  современного танца»** — это не балет.
- «Четыре пластических высказывания» → **три части**: «Гендер»
  (танцдраматург Анастасия Сенокосова), «Иерархия» (танцдраматург
  Иван Сачков), «Ритуал» (танцдраматург Юлия Бычкова). Раньше
  Сенокосова на сайте Аэлиты не упоминалась вообще.
- `/sachkov` — было ложное «хореограф и танцдраматург» /
  «в соавторстве с Юлией Бычковой» (программу целиком делает Бычкова
  как режиссёр программы, Сачков — танцдраматург только «Иерархии»).
  Поправлено.
- `/koroche` — карточка и таймлайн спектакля обновлены тем же образом
  («Готовится к премьере» → «Премьера состоялась», per факту с сайта
  «Короче»).
- `/tochkacuiri` — убрано «продюсерская компания из Калуги» (решение
  убрать привязку к Калуге распространяется и на этот текст).

## Каталог проектов и резиденты (`aelita-v13`)

- `CUIRI FEST` в каталоге `AELITA_final.pptx` был дублем «Точки Кюри»
  — слоты объединены, свободная 12-я ячейка занята пунктом
  `AELITA COMMUNITY` (как в `Documents/Print/AELITA_projects.pptx`)
  для баланса сетки.
- «Гоголь.Сюита» и «БудуТанцы» помечены «АРХИВ» в каталоге
  `AELITA_final.pptx` (на сайте это уже было верно).
- Дата фестиваля «Точка Кюри» (22–25 октября 2026) подставлена вместо
  «Дата уточняется» на слайдах «Афиша 2026» в `AELITA_final.pptx` и
  `IOV_material.pptx` — для показа именно в рамках этого фестиваля;
  показы без объявленной даты (Порфирий-тур, ИОВ в Александринке)
  остались как есть.
- Счётчики проектов приведены к единому «10+» (было: «10+», «десять»,
  «12 постановок», «10» в разных материалах).
- Резиденты рассинхронизировались между сайтом (Абдулова, Бычкова,
  Ильина, Сачков, Дыба) и `AELITA_final.pptx` (Абдулова, Бычкова,
  Ильина, Панов) — добавлен Панов на сайт (`/rezidents`, фото временно
  как заглушка — реального фото нет ни на одной странице сайта),
  Сачков и Дыба добавлены третьим рядом в презентацию.

## Реквизиты и подписи (`aelita-v13`)

- Должности Абдуловой и Мошникова были в трёх разных редакциях
  (сайт/CV/email-подпись) — приведены к единой формулировке в CV и во
  всех email-подписях: Абдулова — «Основательница и генеральный
  директор · Продюсер, режиссёр» (сайт не трогал — там уже была
  наиболее подробная версия); Мошников — «Сооснователь · Продюсер ·
  SMM».
- Личная подпись Мошникова использовала общий телефон компании
  (+7 960 515-16-90) вместо его собственного — заменено на
  +7 904 617-01-88 (как в его CV).
- Дзен отсутствовал во всех трёх email-подписях (был на сайте и в
  презентациях) — добавлен.
- На `/moshnikov` и в его личную подпись добавлена ссылка
  `orgculture.ru`.

## Контрольная вычитка (`aelita-v14`)

Сквозной поиск по всему паку показал четыре места, где «Археология
человеческого» осталась описана по-старому мимо страницы `/human`:
`/tochkacuiri` (программа фестиваля), `/business` (карточка репертуара,
последний оставшийся `18+`), `/projects` (карточка каталога) и
`/bychkova` (жанр + ложное «в соавторстве с Иваном Сачковым» — тот же
дефект, что чинился на `/human` и `/sachkov`, но сформулированный в
обратную сторону). Все поправлены.

**Урок:** факты о спектакле живут не только на его собственной
странице. При правке описания/жанра/возраста любого спектакля надо
проверять минимум: его страницу, `/projects`, `/tochkacuiri` (программа
фестиваля), `/business` (карточки репертуара для корпоративных
показов), страницы всех причастных персон (`/bychkova`, `/sachkov`,
`/ilyina` и т.д.) и `/koroche`. Быстрее всего — grep по названию
спектакля через весь `Site/`, а не по одной странице.

## Известные дефекты вёрстки (не внесены правками v13–v14)

В `iov/index.html`, `moshnikov/index.html` и `projects/index.html`
несбалансированы `<div>` (лишний закрывающий в `iov`, незакрытый в
двух других). Проверено сверкой с исходным паком — дефект был там
и раньше, правками v13–v14 не вносился. Браузер закрывает такие теги
сам, на вид страниц не влияет, но при следующей содержательной правке
этих трёх файлов стоит починить.

## Исправлен баланс `<div>` (`aelita-v15`)

Три давних дефекта вёрстки (были ещё в исходном паке, не внесены
правками v13–v14) — починены:

- `iov/index.html` — в секции «Фотографии» был **лишний** закрывающий
  `</div>`: блок `.wrap` закрывался ещё до сетки фото, а в конце секции
  стояло на один `</div>` больше, чем открыто.
- `moshnikov/index.html` — в секции «Работы в компании» не закрывался
  `<div class="wrap">` перед `</section>`.
- `projects/index.html` — не закрывался внешний `<div class="wrap">`,
  открытый перед блоком «Флагман»: последний `</div>` в файле закрывал
  `.cat-section`, а на `.wrap` закрывающего не оставалось.

Браузер такие теги закрывает сам, поэтому на вид страниц это не влияло,
но ломало любой инструмент, который парсит разметку всерьёз. Проверено:
все 42 страницы теперь сбалансированы полностью.

## Переезд `/tochkacuiri` → `/tochkacuire` (`aelita-v15`)

Адрес страницы фестиваля изменён. Обновлены все 21 вхождение: ссылки со
страниц (`index`, `about`, `iov`, `moshnikov`, `projects`, `tickets`),
`canonical` и `og:url` на самой странице, `url` в JSON-LD на главной,
`<loc>` в `sitemap.xml` и путь в списке `PRECACHE_URLS` в `sw.js`.

**Важно — `/tochkacuiri/` оставлен как страница-редирект.** Старый
адрес уже был в `sitemap.xml` предыдущих версий, то есть проиндексирован
и может стоять во внешних ссылках — при простом переименовании он начал
бы отдавать 404. Поэтому по старому пути лежит заглушка: `canonical` на
новый адрес, `noindex, follow`, мгновенный редирект (meta refresh +
`location.replace`). Содержимого в ней нет и добавлять не нужно.

Это **исключение из правила «папка есть → она в `sitemap.xml`»**: в
sitemap редирект-заглушка не входит намеренно (её задача — увести
пользователя и передать вес поисковику, а не индексироваться самой).
Когда старый адрес окончательно выпадет из выдачи, папку `tochkacuiri/`
можно будет просто удалить.
